const Order = require('../../models/order');

const createOrder = ({}) =>
  new Promise(async (resolve, reject) => {
    try {
    } catch (err) {
      reject(err);
    }
  });

module.exports = { createOrder };
